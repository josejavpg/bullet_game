<h1>Ranking#index</h1>
<%#<%= link_to 'Home', path %>
<%= link_to 'Jugar', :game %>

<table>
  <tr>
    <th>Nickname</th>
    <th>Puntos</th>
  </tr>
  <% @ranking.each do |player| %>
    <tr>
      <td><%= player.nickname %></td>
      <td><%= player.points %></td>
    </tr>
  <% end %>
</table>
